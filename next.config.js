/** @type {import('next').Next.js').Config} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true, // For easier deployment if needed
  },
};

module.exports = nextConfig;
